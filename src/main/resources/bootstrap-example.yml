spring:
  profiles:
    active: ${ENVIRONMENT_PROFILE_NAME:dev,local}
  application:
    name: mission-control
  cloud:
    vault:
      fail-fast: false
      generic.enabled: false
      kv.enabled: false
      authentication: NONE